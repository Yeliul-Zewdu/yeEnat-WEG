/**
 * API configuration.
 * Base URL for the deployed YeEnat Weg backend.
 */
export const API_BASE_URL = "https://enateweg.onrender.com/v1";

/** OTP value the backend accepts in MVP (auth.controller hardcodes this). */
export const MVP_OTP = "123456";
